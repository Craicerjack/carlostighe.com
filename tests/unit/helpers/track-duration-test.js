import { trackDuration } from 'carlostighe/helpers/track-duration';
import { module, test } from 'qunit';

module('Unit | Helper | track duration');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trackDuration([42]);
  assert.ok(result);
});
