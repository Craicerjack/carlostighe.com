import { splitRow } from 'marvel/helpers/split-row';
import { module, test } from 'qunit';

module('Unit | Helper | split row');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = splitRow([42]);
  assert.ok(result);
});
